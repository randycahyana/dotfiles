[user]
  name = Randy Cahyana
  email = ranren.cahyana@gmail.com

[core]
  editor = nvim

[alias]
  s = status
  a = add
  c = commit
  d = diff
  r = reset
  rb = rebase
  co = checkout
  cp = cherry-pick
  lg = log
  ls = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%C(bold blue)<%an>%Creset' --abbrev-commit
  dt = diff-tree --no-commit-id --name-only -r
  assume = update-index --assume-unchanged
  unassume = update-index --no-assume-unchanged
  flat = clone --depth 1

  # git-codereview aliases
  change = codereview change
  gofmt = codereview gofmt
  mail = codereview mail
  pending = codereview pending
  submit = codereview submit
  sync = codereview sync

[push]
  default = simple

[branch]
  autosetuprebase = always

[includeIf "gitdir:~/workspace/go/src/brank.as/"]
  path = .gitconfig-work

[includeIf "gitdir:~/workspace/go/src/gitlab.bri.co.id/"]
  path = .gitconfig-work-bri

[http]
  cookiefile = /home/randy/.gitcookies
